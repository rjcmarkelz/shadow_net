# shadow_net
# network modeling with RNA-seq data
# basic graph model first

library(lavaan)

ls()
shade <- SAS.expression.vst.s.kazu.largeCV.modules
head(shade)
tail(shade)

# drew out model
# translate to pseudo code on white board.